<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ciastkaipiwo.android.scrummajster">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistrationActivity">

        </activity>
        <activity android:name=".ProjectConfigActivity" />
        <activity android:name=".ProjectActivity" />
        <activity android:name=".SprintConfigActivity" />
        <activity android:name=".SprintActivity" />
        <activity
            android:name=".KanbanActivity"
            android:label="Kanban" />
        <activity
            android:name=".BacklogActivity"
            android:label="Backlog" />
        <activity android:name=".TaskConfigActivity" />
        <activity
            android:name=".SprintMainActivity"
            android:label="Sprints" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>